## Exemplary Verilog IPs

These Verilog modules provide synthesizable 1-to-1 replacements for the according NEORV32 VHDL modules.
Hence, they provide the same file name (but with a `.v` suffix), the same module name and the same interface
(generics/parameters and ports).
